*/Implementación de la función factorial de un numero, hecha en reverse/*

}
    quedarse 1;
indefinir xd(){

}
    };
        quedarse (x / res);
        res != colgar factorial(inc);
        inc != (x+1);
        a != ("str"-1);
    }ademas{
        quedarse 1;
    sino (x !== 1){
indefinir factorial(x){

fin
	colgar ocultar(res);
	res != colgar factorial(5);
inicio
