*/Implementación de la función factorial de un numero, hecha en reverse/*

}
    };
        quedarse (x / res);
        res != colgar factorial(inc);
        inc != (x-1);
    }ademas{
        quedarse 1;
    sino (x !== 1 o x){
indefinir factorial(x){
fin
	colgar factorial(5);
inicio